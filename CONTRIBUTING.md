# 貢獻指南

感謝你參與台灣交通改革協作平台。

## 回報道路問題

請使用 [GitHub Issue Form](https://github.com/wy-zong/taiwan-road-reform/issues/new/choose)。

### 必填資訊

| 欄位 | 說明 |
|---|---|
| 地點 | 具體地址或路口名稱 |
| 縣市 | 從下拉選單選擇 |
| 問題類型 | 從下拉選單選擇，沒有合適選項時選擇「其他」 |
| 問題描述 | 說明問題、影響及現場狀況 |

### 建議提供

- 現場照片
- Google Maps 或 OpenStreetMap 分享連結
- 可能的主管機關
- 事故、通行障礙或過往陳情資訊

地圖分享連結若包含座標，網站會顯示精確位置；否則只以縣市彙總位置呈現。

## 案件標籤

系統會從 Issue Form 欄位維護一個縣市標籤、一個問題類型標籤及一個狀態標籤。請不要同時加入多個狀態；若加入新狀態，系統會移除舊狀態。

### 狀態標籤

| 標籤 | 說明 |
|---|---|
| `狀態/待確認` | 新回報，等待社群確認 |
| `狀態/已確認` | 問題已確認存在 |
| `狀態/已陳情` | 已向主管機關反映 |
| `狀態/處理中` | 主管機關正在處理 |
| `狀態/已解決` | 問題已完成改善 |
| `狀態/無法解決` | 案件已結束但未能改善 |

## 歸檔已解決案例

### 前提條件

- 原 Issue 存在且地點相符
- 問題確實已改善
- 有足以辨識改善結果的照片或資料

### 步驟

1. Fork 本專案。
2. 在 `resolved/` 建立 `YYYY-MM-縣市-簡短描述.md`。
3. 使用以下格式記錄案例：

```markdown
# [地點] [問題簡述]

| 項目 | 內容 |
|---|---|
| 原始 Issue | #123 |
| 地點 | 完整地址 |
| 問題類型 | 人行道／行穿線／其他 |
| 通報日期 | YYYY-MM-DD |
| 解決日期 | YYYY-MM-DD |
| 負責單位 | XX市政府XX局 |

## 問題描述

## 改善內容

## 照片對比

| 改善前 | 改善後 |
|---|---|
| ![before](連結) | ![after](連結) |
```

4. 提交 PR，並在 PR 描述中使用 `Closes #123`。
5. 建議取得至少五位參與者確認；最終由維護者依證據完整度決定是否合併。

## 程式與網站貢獻

1. 從 `main` 建立功能分支。
2. 安裝並準備本機資料：

   ```bash
   npm install
   npm run prepare:local
   ```

3. 完成修改後執行：

   ```bash
   npm run check
   ```

4. 不要提交 `website/data/`；該目錄由本機指令或 GitHub Actions 產生。
5. 若新增縣市、問題類型或狀態，請修改 `config/taxonomy.json`，並同步更新 Issue Form。

## 常見問題

### 不知道負責單位怎麼辦？

可以留空，社群或維護者後續補充。

### 為什麼地圖只顯示縣市位置？

Issue 沒有提供可解析的座標時，網站只顯示縣市彙總位置，避免用假座標誤導使用者。

### PR 長時間沒有回應？

可以在 PR 留言並標註 `@wy-zong`。
